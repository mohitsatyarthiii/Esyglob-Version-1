# Browser-facing API path. Keep this same-origin in production.
VITE_API_BASE_URL=/api

# Development-server proxy target. This variable is not bundled into the browser.
ADMIN_API_PROXY=http://localhost:5000
