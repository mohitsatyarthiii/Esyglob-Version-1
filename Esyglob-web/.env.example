# Netlify proxies this same-origin path to the production backend.
VITE_API_BASE_URL=/api

# Keep Socket.IO on the Netlify origin so credentialed realtime requests avoid CORS.
VITE_REALTIME_URL=/

# Used only by the local Vite development proxy.
VITE_API_PROXY_TARGET=https://api.esyglob.in
