/* eslint-disable react-hooks/set-state-in-effect */
import { CalendarClock, CheckCircle2, ClipboardCheck, Factory, RefreshCw, Search } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import AppShell from '../components/AppShell'
import { fetchVerificationReviews, reviewVerification } from '../api/verification'

const statuses=['under_review','additional_information_required','factory_inspection_scheduled','approved','rejected','reverification_required']
export default function VerificationAdminPage(){const [items,setItems]=useState([]),[selected,setSelected]=useState(null),[status,setStatus]=useState('under_review'),[notes,setNotes]=useState(''),[inspection,setInspection]=useState(''),[search,setSearch]=useState(''),[message,setMessage]=useState('');const load=useCallback(async()=>{const result=await fetchVerificationReviews({status:'all',search});setItems(result?.verifications||result||[])},[search]);useEffect(()=>{load().catch(e=>setMessage(e.message))},[load]);async function submit(){try{await reviewVerification(selected._id,{status,notes,inspectionScheduledAt:inspection||undefined});setMessage('Application updated');setSelected(null);setNotes('');load()}catch(e){setMessage(e.message)}}return <AppShell><main className="container admin-verification-page"><header className="verification-hero"><div><span className="eyebrow">Trust operations</span><h1>Verification review queue</h1><p>Review evidence, request information, schedule inspections and approve verified suppliers.</p></div><div className="verification-status"><ClipboardCheck/><span><small>Applications</small><b>{items.length} in queue</b></span></div></header>{message&&<div className="verification-alert">{message}<button onClick={()=>setMessage('')}>Dismiss</button></div>}<label className="admin-review-search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search company or document"/></label><div className="admin-review-layout"><section className="admin-review-list">{items.map(item=><button className={selected?._id===item._id?'active':''} onClick={()=>{setSelected(item);setStatus(item.status==='submitted'?'under_review':item.status)}} key={item._id}><i><Factory/></i><span><b>{item.sellerId?.companyName||'Seller application'}</b><small>{String(item.status).replaceAll('_',' ')} · {item.documents?.length||0} documents</small></span><strong>{item.overallTrustScore||0}</strong></button>)}</section><section className="module-panel admin-review-detail">{selected?<><h2>{selected.sellerId?.companyName}</h2><p>Submitted {new Date(selected.submittedAt||selected.createdAt).toLocaleString()}</p><div className="admin-doc-list">{selected.documents?.filter(x=>x.status!=='archived').map(doc=><a key={doc._id} href={`/api/suppliers/verification/documents/${doc._id}`} target="_blank" rel="noreferrer"><CheckCircle2/><span><b>{doc.name}</b><small>{doc.type.replaceAll('_',' ')} · {doc.status}</small></span></a>)}</div><label><span>Decision</span><select value={status} onChange={e=>setStatus(e.target.value)}>{statuses.map(x=><option value={x} key={x}>{x.replaceAll('_',' ')}</option>)}</select></label>{status==='factory_inspection_scheduled'&&<label><span><CalendarClock/> Inspection date</span><input type="datetime-local" value={inspection} onChange={e=>setInspection(e.target.value)}/></label>}<label><span>Reviewer notes</span><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Explain the decision or requested changes"/></label><button className="button button--primary button--full" onClick={submit}><RefreshCw/>Update application</button></>:<div className="empty-timeline"><ClipboardCheck/><p>Select an application to begin review.</p></div>}</section></div></main></AppShell>}
