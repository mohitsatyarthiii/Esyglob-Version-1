import { Camera, ImagePlus, RefreshCw, Search, Sparkles, Upload, X } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import { Link } from 'react-router-dom'
import { searchByImage } from '../api/marketplace'
import { uploadFiles } from '../api/trade'
import { useAuth } from '../auth/auth-context'
import AppShell from '../components/AppShell'
import { ManufacturerCard, ProductCard } from '../components/MarketplaceCards'
import { PageHead } from '../components/PageHead'

export default function ImageSearchPage() {
  const { user } = useAuth(); const role = user?.primaryRole || 'buyer'; const cameraRef = useRef(null); const uploadRef = useRef(null)
  const [file, setFile] = useState(null); const [preview, setPreview] = useState(''); const [url, setUrl] = useState(''); const [query, setQuery] = useState('')
  const [state, setState] = useState({ loading: false, error: '', data: null })
  const [dragging, setDragging] = useState(false)
  useEffect(() => { return () => { if (preview.startsWith('blob:')) URL.revokeObjectURL(preview) } }, [preview])
  function acceptFile(next) { if (!next) return; if (!next.type.startsWith('image/')) { setState({ loading: false, error: 'Choose a JPG, PNG or WebP image.', data: null }); return } if (next.size > 5 * 1024 * 1024) { setState({ loading: false, error: 'Image must be smaller than 5MB.', data: null }); return } setFile(next); setPreview(URL.createObjectURL(next)); setUrl(''); setState({ loading: false, error: '', data: null }) }
  function choose(event) { acceptFile(event.target.files?.[0]) }
  function drop(event) { event.preventDefault(); setDragging(false); acceptFile(event.dataTransfer.files?.[0]) }
  function clear() { setFile(null); setPreview(''); setUrl(''); setState({ loading: false, error: '', data: null }); if (cameraRef.current) cameraRef.current.value = ''; if (uploadRef.current) uploadRef.current.value = '' }
  async function submit(event) { event.preventDefault(); if (!file && !url.trim()) return setState({ loading: false, error: 'Upload, capture, or paste an image first.', data: null }); setState({ loading: true, error: '', data: null }); try { let imageUrl = url.trim(); if (file) { const uploads = await uploadFiles([file], 'image-search'); imageUrl = uploads[0]?.secure_url || uploads[0]?.url || uploads[0]?.location; if (!imageUrl) throw new Error('The secure image upload did not return a URL.') } const data = await searchByImage(imageUrl, query, role); setPreview(imageUrl); setState({ loading: false, error: '', data }) } catch (error) { setState({ loading: false, error: error.message, data: null }) } }
  const data = state.data
  const suggestions = (Array.isArray(data?.suggestions) ? data.suggestions : []).map((item) => typeof item === 'string' ? item : item?.prompt || item?.label || item?.title).filter(Boolean)
  return <AppShell><div className="container visual-search-page"><PageHead eyebrow="AI visual sourcing" title="Search the marketplace by image" description="Upload or capture a product photo. EsyGlob identifies visible attributes and searches matching products, categories and suppliers." /><form className="visual-search-workspace" onSubmit={submit}><div className={`visual-dropzone ${preview ? 'has-image' : ''} ${dragging ? 'is-dragging' : ''}`} onDragEnter={(event) => { event.preventDefault(); setDragging(true) }} onDragOver={(event) => event.preventDefault()} onDragLeave={(event) => { if (!event.currentTarget.contains(event.relatedTarget)) setDragging(false) }} onDrop={drop}>{preview ? <><img src={preview} alt="Visual search source" /><button type="button" onClick={clear} aria-label="Remove image"><X /></button></> : <><i><ImagePlus /></i><h2>{dragging ? 'Release to use this image' : 'Drop in a clear product image'}</h2><p>JPG, PNG or WebP · Maximum 5MB</p><div><button type="button" className="button button--primary" onClick={() => uploadRef.current?.click()}><Upload /> Upload image</button><button type="button" className="button button--secondary" onClick={() => cameraRef.current?.click()}><Camera /> Use camera</button></div></>}<input ref={uploadRef} hidden type="file" accept="image/jpeg,image/png,image/webp" onChange={choose} /><input ref={cameraRef} hidden type="file" accept="image/*" capture="environment" onChange={choose} /></div><aside><label>Optional product description<textarea value={query} onChange={(event) => setQuery(event.target.value)} placeholder="e.g. recycled cotton tote bag, bulk order" /></label><span>or use a secure image URL</span><label><input type="url" value={url} disabled={Boolean(file)} onChange={(event) => { setUrl(event.target.value); setPreview(event.target.value) }} placeholder="https://…" /></label><button className="button button--primary button--full" disabled={state.loading || (!file && !url)}><Search /> {state.loading ? 'Analyzing and searching…' : 'Find visual matches'}</button><small><Sparkles /> Results combine visual attributes with live EsyGlob marketplace inventory.</small></aside></form>{state.error && <div className="inline-error">{state.error}</div>}{data && <div className="visual-results"><section className="visual-analysis"><div><Sparkles /></div><span><small>{data.imageSearch?.status === 'analyzed' ? 'VISUAL ANALYSIS COMPLETE' : 'MARKETPLACE SEARCH COMPLETE'}</small><h2>{data.imageSearch?.analysis || 'Intelligent marketplace recommendations'}</h2><p>{data.answer}</p></span><button className="button button--secondary" onClick={clear}><RefreshCw /> New image</button></section>{data.categories?.length > 0 && <section className="result-section"><div className="compact-heading"><h2>Similar categories</h2></div><div className="visual-categories">{data.categories.map((item) => <Link key={item._id || item.id || item.slug} to={`/categories/${item._id || item.id || item.slug}`}>{item.name || item.title}</Link>)}</div></section>}{data.products?.length > 0 && <section className="result-section"><div className="compact-heading"><h2>Similar products</h2><span>{data.products.length} matches</span></div><div className="product-grid">{data.products.map((item) => <ProductCard key={item._id || item.id} product={item} />)}</div></section>}{data.sellers?.length > 0 && <section className="result-section"><div className="compact-heading"><h2>Matching suppliers</h2><span>{data.sellers.length} matches</span></div><div className="manufacturer-grid">{data.sellers.map((item) => <ManufacturerCard key={item._id || item.id} seller={item} />)}</div></section>}{!data.products?.length && !data.sellers?.length && <div className="empty-results"><Camera /><h2>No strong marketplace matches</h2><p>Retake the photo in good light with one product centered, or add a short description.</p></div>}{suggestions.length > 0 && <section className="visual-recommendations"><h2>Recommended next steps</h2>{suggestions.map((item) => <button key={item} type="button" onClick={() => setQuery(item)}>{item}</button>)}</section>}</div>}</div></AppShell>
}
